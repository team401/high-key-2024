package frc.robot.constants;

public class FeatureFlagsSchema {
    public static boolean runDrive = true;
    public static boolean runVision = false;
    // NOTE: A featureflag "runLocalizer" was removed from here recently.
    // TODO: Figure out if we need this and add it back if necessary.
    public static boolean runLocalizer = false;
    public static boolean runIntake = true;
    public static boolean runScoring = true;
    public static boolean runLEDS = true;
    public static boolean runOrchestra = true;
}
